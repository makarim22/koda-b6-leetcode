// 2619 : array prototype last

Array.prototype.last = function() {
   if(this.length > 0) {
       return this[this.length - 1];
   } else {
     return -1
   }
};

const arr = [];
console.log(arr.last());

